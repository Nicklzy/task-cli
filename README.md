# to-do-cli
> A command line tool that allows you to create a To-Do lists.
## Documentation
```shell
npm install -g to-do-cli
```
## Usage
```shell
t
```
```shell
> exit
  [x] buy food
  + add a task

```
